<Window x:Class="ScreenForge.Windows.QuickTranslateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hızlı çeviri"
        Width="548"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="{StaticResource UiFont}"
        PreviewKeyDown="OnPreviewKeyDown">
    <!--
        Hallmark · genre: modern-minimal · component: command-palette
        · theme: ScreenForge (warm-orange / dark glass)
        · pre-emit critique: P4 H5 E4 S5 R4 V4
        DropShadowEffect kare sınır çizer; gölge ayrı yuvarlak katmanda.
    -->
    <Window.Resources>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="7"/>
            <Setter Property="MinWidth" Value="7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Margin="0,10,4,10">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="tb" CornerRadius="4" Width="5"
                                                        HorizontalAlignment="Center"
                                                        Background="#55A8B4C8"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="tb" Property="Background" Value="#88D0D7E4"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="tb" Property="Background" Value="#BBEA6F12"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="Transparent" MouseLeftButtonDown="OnOutsideClick">
        <Border Margin="16,12,16,22" CornerRadius="18"
                Background="#99000000" IsHitTestVisible="False">
            <Border.Effect>
                <BlurEffect Radius="24"/>
            </Border.Effect>
        </Border>

        <Border x:Name="RootCard"
                Margin="24,16,24,20"
                BorderThickness="1"
                CornerRadius="16"
                SnapsToDevicePixels="False"
                SizeChanged="OnRootCardSizeChanged"
                MouseLeftButtonDown="OnChromeMouseDown">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#55FFFFFF" Offset="0"/>
                    <GradientStop Color="#18FFFFFF" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Background>
                <SolidColorBrush Color="#FF1E2430"/>
            </Border.Background>

            <Border CornerRadius="15" BorderThickness="1">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#40EA6F12" Offset="0"/>
                        <GradientStop Color="#00EA6F12" Offset="0.35"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <StackPanel>
                    <Border Padding="18,14,10,14">
                        <Grid MinHeight="22">
                            <TextBox x:Name="TxtInput"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Padding="0"
                                     Margin="0"
                                     Foreground="{StaticResource TextBrush}"
                                     CaretBrush="{StaticResource AccentBrush}"
                                     FontSize="15"
                                     FontFamily="{StaticResource UiFont}"
                                     VerticalContentAlignment="Center"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     MaxHeight="112"
                                     TextChanged="OnInputTextChanged">
                                <TextBox.Template>
                                    <ControlTemplate TargetType="TextBox">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                                      Margin="0" Padding="0"
                                                      VerticalAlignment="Center"
                                                      CanContentScroll="False"
                                                      HorizontalScrollBarVisibility="Disabled"
                                                      VerticalScrollBarVisibility="Auto"/>
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                            <TextBlock x:Name="TxtPlaceholder"
                                       Text="Çevir…"
                                       Foreground="#99A8B4C8"
                                       FontSize="15"
                                       FontFamily="{StaticResource UiFont}"
                                       VerticalAlignment="Center"
                                       Margin="0"
                                       Padding="0"
                                       IsHitTestVisible="False"/>
                        </Grid>
                    </Border>

                    <Border x:Name="ResultHost" Visibility="Collapsed">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#22000000" Offset="0"/>
                                <GradientStop Color="#14000000" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Height="1" Margin="18,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#00FFFFFF" Offset="0"/>
                                        <GradientStop Color="#28FFFFFF" Offset="0.5"/>
                                        <GradientStop Color="#00FFFFFF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <Grid Grid.Row="1">
                                <ScrollViewer x:Name="ResultScroll"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled"
                                              Padding="0,0,2,0">
                                    <TextBox x:Name="TxtResult"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="18,12,58,14"
                                             Margin="0"
                                             Foreground="#E8ECF2"
                                             CaretBrush="{StaticResource AccentBrush}"
                                             FontSize="14"
                                             FontFamily="{StaticResource UiFont}"
                                             IsReadOnly="True"
                                             TextWrapping="Wrap"
                                             AcceptsReturn="True">
                                        <TextBox.Template>
                                            <ControlTemplate TargetType="TextBox">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="0" Padding="0"/>
                                            </ControlTemplate>
                                        </TextBox.Template>
                                    </TextBox>
                                </ScrollViewer>
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Right" VerticalAlignment="Top"
                                            Margin="0,10,12,0">
                                    <TextBlock x:Name="TxtStatus"
                                               Foreground="#99A8B4C8"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0"/>
                                    <Button x:Name="BtnCopy"
                                            Width="30" Height="26"
                                            Cursor="Hand"
                                            ToolTip="Çeviriyi kopyala"
                                            Click="OnCopyClick">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="bd" CornerRadius="8"
                                                        Background="#33FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="bd" Property="Background" Value="#44EA6F12"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Path x:Name="CopyGlyph"
                                              Data="{StaticResource IconCopy}"
                                              Stroke="#E8ECF2" StrokeThickness="1.7"
                                              StrokeLineJoin="Round"
                                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                              Stretch="Uniform" Width="14" Height="14"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
