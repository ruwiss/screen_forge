<Window x:Class="ScreenForge.Windows.QuickTranslateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hızlı çeviri"
        Width="560"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="{StaticResource UiFont}"
        PreviewKeyDown="OnPreviewKeyDown">
    <Window.Resources>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="7"/>
            <Setter Property="MinWidth" Value="7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Margin="0,10,4,10">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="tb" CornerRadius="4" Width="5"
                                                        HorizontalAlignment="Center"
                                                        Background="#55A8B4C8"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="tb" Property="Background" Value="#88D0D7E4"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="tb" Property="Background" Value="#BBEA6F12"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="Transparent" MouseLeftButtonDown="OnOutsideClick">
        <Border x:Name="RootCard"
                Margin="16,0,16,16"
                BorderThickness="1,0,1,1"
                CornerRadius="0,0,14,14"
                Background="#FF202020"
                BorderBrush="#33FFFFFF"
                SnapsToDevicePixels="True"
                UseLayoutRounding="True"
                MouseLeftButtonDown="OnChromeMouseDown">
            <StackPanel>
                <Border Padding="16,11,10,11">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Path Data="{StaticResource IconTranslate}"
                                  Stroke="{StaticResource AccentBrush}" StrokeThickness="1.8"
                                  StrokeLineJoin="Round"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Stretch="Uniform" Width="16" Height="16"
                                  VerticalAlignment="Center"/>
                            <TextBlock Text="Çeviri"
                                       Foreground="{StaticResource TextBrush}"
                                       FontSize="13" FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"/>
                            <Border x:Name="LangBadge" CornerRadius="6" Margin="8,0,0,0" Padding="8,3"
                                    Background="#2A2A2A" BorderBrush="#33FFFFFF" BorderThickness="1"
                                    VerticalAlignment="Center" Visibility="Collapsed">
                                <TextBlock x:Name="TxtLangs" Text=""
                                           Foreground="#9FB0C9" FontSize="11" FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>
                        <TextBlock x:Name="TxtStatus" Grid.Column="1"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"
                                   TextAlignment="Right"
                                   Margin="12,0,8,0"/>
                        <Button x:Name="BtnClose" Grid.Column="2"
                                Width="28" Height="28" Cursor="Hand"
                                ToolTip="Kapat (Esc)"
                                Click="OnCloseClick">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bd" CornerRadius="7" Background="Transparent">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#33FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <Path Data="{StaticResource IconClose}"
                                  Stroke="#9AA4B8" StrokeThickness="2"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Stretch="Uniform" Width="12" Height="12"/>
                        </Button>
                    </Grid>
                </Border>

                <Border Height="1" Margin="16,0" Background="#1EFFFFFF"/>

                <Border Padding="16,12,16,13">
                    <Grid MinHeight="26">
                        <TextBox x:Name="TxtInput"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="0"
                                 Margin="0"
                                 Foreground="{StaticResource TextBrush}"
                                 CaretBrush="{StaticResource AccentBrush}"
                                 FontSize="14"
                                 FontFamily="{StaticResource UiFont}"
                                 VerticalContentAlignment="Center"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 MaxHeight="120"
                                 TextChanged="OnInputTextChanged">
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <ScrollViewer x:Name="PART_ContentHost"
                                                  Margin="0" Padding="0"
                                                  VerticalAlignment="Center"
                                                  CanContentScroll="False"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Auto"/>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                        <TextBlock x:Name="TxtPlaceholder"
                                   Text="Çevirmek için yazın…"
                                   Foreground="#6B7A90"
                                   FontSize="14"
                                   FontFamily="{StaticResource UiFont}"
                                   Margin="0"
                                   Padding="0"
                                   IsHitTestVisible="False"/>
                    </Grid>
                </Border>

                <Border x:Name="ResultHost" Visibility="Collapsed"
                        Background="#1A1A1A" CornerRadius="0,0,13,13">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Height="1" Margin="16,0" Background="#29EA6F12"/>
                        <Grid Grid.Row="1">
                            <ScrollViewer x:Name="ResultScroll"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled"
                                          Padding="0,0,2,0">
                                <TextBox x:Name="TxtResult"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Padding="16,11,58,13"
                                         Margin="0"
                                         Foreground="#EDF1F7"
                                         CaretBrush="{StaticResource AccentBrush}"
                                         FontSize="14"
                                         FontFamily="{StaticResource UiFont}"
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True">
                                    <TextBox.Template>
                                        <ControlTemplate TargetType="TextBox">
                                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" Padding="0"/>
                                        </ControlTemplate>
                                    </TextBox.Template>
                                </TextBox>
                            </ScrollViewer>
                            <Button x:Name="BtnCopy"
                                    Width="30" Height="30"
                                    HorizontalAlignment="Right" VerticalAlignment="Top"
                                    Margin="0,8,10,0"
                                    Cursor="Hand"
                                    ToolTip="Çeviriyi kopyala"
                                    Click="OnCopyClick">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bd" CornerRadius="8"
                                                Background="#1FFFFFFF" BorderBrush="#29FFFFFF" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#44EA6F12"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                                <Path x:Name="CopyGlyph"
                                      Data="{StaticResource IconCopy}"
                                      Stroke="#E8ECF2" StrokeThickness="1.7"
                                      StrokeLineJoin="Round"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Stretch="Uniform" Width="14" Height="14"/>
                            </Button>
                        </Grid>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>
